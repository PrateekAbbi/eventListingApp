<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!--Font Awesome Links-->
    <script src="https://kit.fontawesome.com/64d58efce2.js" crossorigin="anonymous"></script>
    <link rel="canonical" href="https://getbootstrap.com/docs/4.1/examples/sign-in/">

    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">

    <!--Customized CSS-->
    <link rel="stylesheet" href="/css/home/home.css" />

    <!--Google Font Link-->
    <link rel="preconnect" href="https://fonts.googleapis.com/css2">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=McLaren&display=swap" rel="stylesheet">
    <title>Even Listing App</title>

    <nav class="navbar navbar-default">
        <div class="container">
            <div class="navbar-header">
                <a href="/" class="navbar-brand">Your Events</a>
            </div>
            <ul class="nav navbar-nav navbar-right">
                <li id="addEvent">
                    <a href="/addEvent">Add an Event</a>
                </li>
                <li id="addEvent">
                    <a href="/logout"><i class="fa fa-sign-out fa-2x"></i></a>
                </li>
            </ul>
        </div>
    </nav>
</head>
<body>
    <div class="container">
        <% if (events.length === 0) { %>
            <h1>Hello <%- name.name %>, you have no events as of now. <a href="/addEvent" style="text-decoration: none;">Click here</a> to add some events😊</h1>
        <% } else { %>
            <% if (events.length === 1) { %>
                <h1>Hello <%- name.name %>, your event is</h1><br>
            <% } else { %>
                <h1>Hello <%- name.name %>, your events are</h1><br>
            <% } %> 
        <% } %>
        <% events.forEach(event => { %>
            <h3><%- event.eventTitle %></h3>
            <p>
                <%- event.eventBody.substring(0, 100) + "..." %>
                <a href="/events/<%- event._id %>">Read More</a>
            </p>
        <% }); %>
    </div>
    <div class="footer-padding"></div>
    <div class="footer">
        <p>Made with ❤️ by Event Listers.</p>
    </div>
</body>
</html>